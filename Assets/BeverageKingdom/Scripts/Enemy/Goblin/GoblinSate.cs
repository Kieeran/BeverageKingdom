using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GoblinSate
{
   /* protected PlayerStateMachine stateMachine;
    protected Player player;
    protected string namePlayerState;

    protected Rigidbody2D rb;

    protected float xInput;

    protected int ComboCounter = 0;
    protected float timerState;

    public Goblin(GoblinSat playerStateMachine, Player player, string namePlayerState)
    {
        this.stateMachine = playerStateMachine;
        this.player = player;
        this.namePlayerState = namePlayerState;
    }
    public virtual void Enter()
    {
        player.anim.SetBool(namePlayerState, true);
        rb = player.rb;
    }
    public virtual void Update()
    {
        player.anim.SetFloat("yVelocity", rb.velocity.y);
        player.anim.SetFloat("xVelocity", rb.velocity.y);
        xInput = InputMangager.Instance.Horizontal;
    }
    public virtual void Exit()
    {
        player.anim.SetBool(namePlayerState, false);*/
}   
